# Plan 

Create :

  1. `SampleExtActionFireFighting` && `Decision Tree` && `Documentation`
  2. `SampleExtActionRescue` && `Decision Tree` && `Documentation`

Draw decision tree first instead of direct dump to codes, only understand the higher abstraction then only start coding.
Use the "Hello World" version of police as starting point.
Then use same structure to finish the rest.
